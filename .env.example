# MongoDB Connection String (Required for Vercel deployment)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# For local development only (not needed on Vercel)
PORT=3001
FRONTEND_URL=http://localhost:5173

# For Frontend (Vite) - Only needed if using separate backend
# Leave empty when using Vercel serverless functions
VITE_API_URL=
